<!--
This file was generated by Spark. Do not edit it by hand.
-->
# BB.Parameter.Store.CubDB

A DSL extension for configuring `BB.Parameter.Store.CubDB`.

Add the extension to your robot and declare where the database lives:

```elixir
defmodule MyRobot.Robot do
  use BB, extensions: [BB.Parameter.Store.CubDB.Dsl]

  parameter_store_cubdb do
    data_dir "/root/my_robot_params"
    auto_file_sync true
  end
end
```

Declaring the section is all that's needed — a transformer sets the robot's
`parameter_store` setting from it. Setting `parameter_store` yourself as well
is an error, since only one store can win.


## parameter_store_cubdb
Persist the robot's parameters in a CubDB database.

Declaring this section sets the robot's `parameter_store` setting to
`BB.Parameter.Store.CubDB`.




### Examples
```
parameter_store_cubdb do
  data_dir "/root/my_robot_params"
end

```




### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`data_dir`](#parameter_store_cubdb-data_dir){: #parameter_store_cubdb-data_dir .spark-required} | `String.t` |  | Directory to store the database in, created if it doesn't exist. On Nerves, use a path under `/root`. |
| [`auto_compact`](#parameter_store_cubdb-auto_compact){: #parameter_store_cubdb-auto_compact } | `boolean \| {pos_integer, number}` | `true` | Compact the database automatically. `true`, `false`, or `{min_write_operations, min_dirt_factor}`. |
| [`auto_file_sync`](#parameter_store_cubdb-auto_file_sync){: #parameter_store_cubdb-auto_file_sync } | `boolean` | `true` | Flush the disk buffer on every write. Turning this off trades durability for write throughput. |







<style type="text/css">.spark-required::after { content: "*"; color: red !important; }</style>
